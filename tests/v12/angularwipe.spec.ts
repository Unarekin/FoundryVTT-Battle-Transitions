import { test, expect } from "../fixtures";
import { angularwipe } from "../data/defaults.json";
import { AngularWipeConfiguration } from '../../src/steps/types';
import { easingConfigTests } from "../common.functions";


test.describe("Scene Configuration", () => {
  test("Default Configuration", async ({ page, scene }) => {

  })
});

test.describe("Transition Builder", () => { });

test.describe("Macro API", () => { });

// test.describe("Scene Configuration", () => {
//   test("Default Configuration", async ({ page }) => { });

//   test.describe("Duration", () => { });

//   test.describe("Backgrounds", () => { });

//   test.describe("Easings", () => {
//     easingConfigTests(angularwipe);
//   });
// });

// test.describe("Transition Builder", () => { });

// test.describe("Macro API", () => { });